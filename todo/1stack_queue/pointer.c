#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int* ptr = arr;  // Вказівник ptr починає вказувати на початок масиву arr

    printf("Елементи масиву: ");
    for (int i = 0; i < 5; i++) {
        printf("%d ", *ptr);  // Виводимо значення, на яке вказує вказівник ptr
        ptr++;  // Зсуваємо вказівник на наступний елемент масиву
    }
    printf("\n");

    return 0;
